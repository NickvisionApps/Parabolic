<UserControl
    x:Class="Nickvision.TubeConverter.WinUI.Controls.DownloadRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nickvision.TubeConverter.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" MinWidth="148" Height="72" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          CornerRadius="{ThemeResource ControlCornerRadius}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" Padding="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <FontIcon x:Name="IcnStatus" Grid.Column="0" VerticalAlignment="Center" Margin="2,0,20,0" FontFamily="{StaticResource SymbolThemeFontFamily}" FontSize="20"/>

        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Orientation="Vertical">
            <TextBlock x:Name="TxtTitle"/>

            <TextBlock x:Name="TxtStatus" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

            <ProgressBar x:Name="ProgBar" HorizontalAlignment="Stretch"/>
        </StackPanel>

        <local:ViewStack x:Name="ViewStackButtons" Grid.Column="3">
            <local:ViewStack.Pages>

            </local:ViewStack.Pages>
        </local:ViewStack>
    </Grid>
</UserControl>
