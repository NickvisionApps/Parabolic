<UserControl
    x:Class="NickvisionTubeConverter.WinUI.Controls.DownloadRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NickvisionTubeConverter.WinUI.Controls"
    xmlns:nickvision="using:NickvisionTubeConverter.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wct="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    mc:Ignorable="d">

    <Border Margin="-14,6,-6,0" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="8">
        <wct:DockPanel Margin="4,4,4,4" LastChildFill="True">
            <FontIcon x:Name="Icon" wct:DockPanel.Dock="Left" VerticalAlignment="Center" FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE118;"/>

            <Button x:Name="BtnStop" wct:DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button.Content>
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE15B;"/>
                </Button.Content>
            </Button>

            <Button x:Name="BtnRetry" wct:DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button.Content>
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE149;"/>
                </Button.Content>
            </Button>

            <Button x:Name="BtnOpenSaveFolder" wct:DockPanel.Dock="Right" VerticalAlignment="Center">
                <Button.Content>
                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE838;"/>
                </Button.Content>
            </Button>

            <StackPanel wct:DockPanel.Dock="Left" Margin="10,0,10,0" Orientation="Vertical" Spacing="2">
                <TextBlock x:Name="TxtFilename" Text="Test.mp3"/>

                <TextBlock x:Name="TxtUrl" Foreground="Gray" Text="https://www.youtube.com/watch?v="/>

                <ProgressBar x:Name="ProgBar" HorizontalAlignment="Stretch" Height="10" IsIndeterminate="True"/>

                <TextBlock x:Name="TxtStatus" Text="Status"/>
            </StackPanel>
        </wct:DockPanel>
    </Border>
</UserControl>
