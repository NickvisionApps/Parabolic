<Window
    x:Class="Nickvision.Parabolic.WinUI.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nickvision.Parabolic.WinUI.Views"
    xmlns:nickvision="using:Nickvision.Desktop.WinUI.Controls"
    xmlns:community="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid x:Name="MainGrid" Loaded="Window_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="TitleBar" Grid.Row="0" IsPaneToggleButtonVisible="True" PaneToggleRequested="TitleBar_PaneToggleRequested">
            <TitleBar.LeftHeader>
                <ImageIcon x:Name="TitleBarIcon" VerticalAlignment="Center" Margin="0,0,8,0" Height="18" Source="/Assets/org.nickvision.tubeconverter.ico"/>
            </TitleBar.LeftHeader>
        </TitleBar>

        <InfoBar x:Name="InfoBar" Grid.Row="1" Margin="6,0,6,6" IsOpen="False">
            <InfoBar.ActionButton>
                <Button x:Name="BtnInfoBar" HorizontalAlignment="Right" Visibility="Collapsed"/>
            </InfoBar.ActionButton>
        </InfoBar>

        <NavigationView x:Name="NavView" Grid.Row="2" SelectionChanged="NavView_SelectionChanged"
                        IsBackButtonVisible="Collapsed" IsPaneToggleButtonVisible="False" IsSettingsVisible="False">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="NavItemHome" Tag="Home" IsSelected="True">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE80F;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="NavItemDownloads" Tag="Downloads">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE896;"/>
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="BadgeDownloads" Visibility="Collapsed"/>
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>

                <NavigationViewItem x:Name="NavItemHistory" Tag="History">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE81C;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="NavItemKeyring" Tag="Keyring">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xF540;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="NavItemUpdates" Visibility="Collapsed" SelectsOnInvoked="False" Tapped="NavItem_Tapped">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xEBD3;"/>
                    </NavigationViewItem.Icon>

                    <NavigationViewItem.InfoBadge>
                        <InfoBadge Style="{StaticResource AttentionDotInfoBadgeStyle}"/>
                    </NavigationViewItem.InfoBadge>

                    <FlyoutBase.AttachedFlyout>
                        <Flyout x:Name="FlyoutProgress">
                            <nickvision:StatusPage x:Name="StsProgress" IsCompact="True">
                                <nickvision:StatusPage.Child>
                                    <ProgressBar x:Name="BarProgress" Width="200"/>
                                </nickvision:StatusPage.Child>
                            </nickvision:StatusPage>
                        </Flyout>
                    </FlyoutBase.AttachedFlyout>
                </NavigationViewItem>

                <NavigationViewItem x:Name="NavItemHelp" SelectsOnInvoked="False" Tapped="NavItem_Tapped">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE897;"/>
                    </NavigationViewItem.Icon>

                    <FlyoutBase.AttachedFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem x:Name="MenuCheckForUpdates" Click="CheckForUpdates">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE895;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutSeparator/>

                            <MenuFlyoutItem x:Name="MenuGitHubRepo" Click="GitHubRepo">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE943;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem x:Name="MenuReportABug" Click="ReportABug">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xEBE8;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutItem x:Name="MenuDiscussions" Click="Discussions">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE8BD;"/>
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>

                            <MenuFlyoutSeparator/>

                            <MenuFlyoutItem x:Name="MenuAbout" Click="About">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE946;"/>
                                </MenuFlyoutItem.Icon>

                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="None" Key="F1"/>
                                </MenuFlyoutItem.KeyboardAccelerators>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </FlyoutBase.AttachedFlyout>
                </NavigationViewItem>

                <NavigationViewItem x:Name="NavItemSettings" Tag="Settings">
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE713;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <nickvision:ViewStack x:Name="ViewStack" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <nickvision:ViewStack.Pages>
                    <nickvision:StatusPage x:Name="StatusHome" Image="/Assets/org.nickvision.tubeconverter.ico">
                        <nickvision:StatusPage.Child>
                            <Button Style="{ThemeResource AccentButtonStyle}" Click="AddDownload">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE710;"/>

                                    <TextBlock x:Name="LblHomeAddDownload"/>
                                </StackPanel>

                                <Button.KeyboardAccelerators>
                                    <KeyboardAccelerator Modifiers="Control" Key="N"/>
                                </Button.KeyboardAccelerators>
                            </Button>
                        </nickvision:StatusPage.Child>
                    </nickvision:StatusPage>

                    <Grid RowSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock x:Name="LblDownloads" Grid.Row="0" Margin="24,24,0,0" Style="{ThemeResource SubtitleTextBlockStyle}"/>

                        <Grid Grid.Row="1" Margin="24,0,24,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <community:TokenView x:Name="DownloadsFilter" Grid.Column="0" SelectionMode="Single" SelectionChanged="DownloadsFilter_SelectionChanged">
                                <community:TokenItem x:Name="FilterAll">
                                    <community:TokenItem.Icon>
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE71D;" />
                                    </community:TokenItem.Icon>
                                </community:TokenItem>

                                <community:TokenItem x:Name="FilterRunning">
                                    <community:TokenItem.Icon>
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE768;" />
                                    </community:TokenItem.Icon>
                                </community:TokenItem>

                                <community:TokenItem x:Name="FilterQueued">
                                    <community:TokenItem.Icon>
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE917;" />
                                    </community:TokenItem.Icon>
                                </community:TokenItem>

                                <community:TokenItem x:Name="FilterCompleted">
                                    <community:TokenItem.Icon>
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE73E;" />
                                    </community:TokenItem.Icon>
                                </community:TokenItem>

                                <community:TokenItem x:Name="FilterFailed">
                                    <community:TokenItem.Icon>
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE783;" />
                                    </community:TokenItem.Icon>
                                </community:TokenItem>
                            </community:TokenView>

                            <StackPanel Grid.Column="2" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="6">
                                <Button Style="{ThemeResource AccentButtonStyle}" Click="AddDownload">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE710;" FontSize="18"/>

                                        <TextBlock x:Name="LblDownloadsAddDownload"/>
                                    </StackPanel>

                                    <Button.KeyboardAccelerators>
                                        <KeyboardAccelerator Modifiers="Control" Key="N"/>
                                    </Button.KeyboardAccelerators>
                                </Button>

                                <CommandBar DefaultLabelPosition="Right">
                                    <CommandBar.SecondaryCommands>
                                        <AppBarButton x:Name="BtnStopAllRemaining" Click="StopAllRemaining">
                                            <AppBarButton.Icon>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE71A;"/>
                                            </AppBarButton.Icon>

                                            <AppBarButton.KeyboardAccelerators>
                                                <KeyboardAccelerator Modifiers="Control,Shift" Key="S"/>
                                            </AppBarButton.KeyboardAccelerators>
                                        </AppBarButton>

                                        <AppBarButton x:Name="BtnRetryAllFailed" Click="RetryAllFailed">
                                            <AppBarButton.Icon>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE72C;"/>
                                            </AppBarButton.Icon>

                                            <AppBarButton.KeyboardAccelerators>
                                                <KeyboardAccelerator Modifiers="Control,Shift" Key="R"/>
                                            </AppBarButton.KeyboardAccelerators>
                                        </AppBarButton>

                                        <AppBarSeparator/>

                                        <AppBarButton x:Name="BtnClearAllQueued" Click="ClearAllQueued">
                                            <AppBarButton.Icon>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE894;"/>
                                            </AppBarButton.Icon>
                                        </AppBarButton>

                                        <AppBarButton x:Name="BtnClearAllCompleted" Click="ClearAllCompleted">
                                            <AppBarButton.Icon>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE894;"/>
                                            </AppBarButton.Icon>
                                        </AppBarButton>
                                    </CommandBar.SecondaryCommands>
                                </CommandBar>
                            </StackPanel>
                        </Grid>

                        <nickvision:ViewStack x:Name="ViewStackDownloads" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                            <nickvision:ViewStack.Pages>
                                <nickvision:StatusPage x:Name="StatusNoneDownloads" Glyph="&#xE896;"/>

                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ItemsRepeater x:Name="DownloadsList" Margin="24,0,24,12">
                                        <ItemsRepeater.Layout>
                                            <StackLayout Spacing="6"/>
                                        </ItemsRepeater.Layout>
                                    </ItemsRepeater>
                                </ScrollViewer>
                            </nickvision:ViewStack.Pages>
                        </nickvision:ViewStack>
                    </Grid>

                    <Frame x:Name="FrameCustom"/>
                </nickvision:ViewStack.Pages>
            </nickvision:ViewStack>
        </NavigationView>

        <ContentDialog x:Name="DlgCredential" DefaultButton="Primary">
            <StackPanel Orientation="Vertical" Spacing="12">
                <TextBlock x:Name="LblCredentialRequired"/>

                <TextBox x:Name="TxtCredentialUrl" IsReadOnly="True"/>

                <TextBox x:Name="TxtCredentialUsername"/>

                <PasswordBox x:Name="TxtCredentialPassword"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Window>
