<Page
    x:Class="Nickvision.Parabolic.WinUI.Views.KeyringPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nickvision.Parabolic.WinUI.Views"
    xmlns:nickvision="using:Nickvision.Desktop.WinUI.Controls"
    xmlns:community="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="LblKeyring" Grid.Row="0" Margin="24,24,0,0" Style="{ThemeResource SubtitleTextBlockStyle}"/>

        <Grid Grid.Row="1" Margin="24,0,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="6">
                <Button x:Name="BtnAdd" Style="{ThemeResource AccentButtonStyle}" Click="Add">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE710;" FontSize="18"/>

                            <TextBlock x:Name="LblAdd"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>

        <AutoSuggestBox x:Name="TxtSearch" Grid.Row="2" Margin="24,0,24,0" HorizontalAlignment="Stretch" QueryIcon="Find" TextChanged="TxtSearch_TextChanged"/>

        <nickvision:ViewStack x:Name="ViewStack" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <nickvision:ViewStack.Pages>
                <nickvision:StatusPage x:Name="StatusNone" Glyph="&#xF540;"/>

                <nickvision:StatusPage x:Name="StatusNoneSearch" Glyph="&#xE721;"/>

                <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ListView x:Name="ListCredentials"  Margin="6,0,6,0" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <community:SettingsCard Margin="0,0,0,6" Header="{Binding Label, Mode=OneWay}" Description="{Binding Value.Url, Mode=OneWay}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <Button Click="Edit" Tag="{Binding Value, Mode=OneWay}">
                                            <Button.Content>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE70F;" FontSize="18"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Click="Remove" Tag="{Binding Value, Mode=OneWay}">
                                            <Button.Content>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE74D;" FontSize="18"/>
                                            </Button.Content>
                                        </Button>
                                    </StackPanel>
                                </community:SettingsCard>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </nickvision:ViewStack.Pages>
        </nickvision:ViewStack>

        <ContentDialog x:Name="DlgCredential" DefaultButton="Primary">
            <StackPanel Orientation="Vertical" Spacing="12">
                <TextBox x:Name="TxtCredentialName"/>

                <TextBox x:Name="TxtCredentialUrl"/>

                <TextBox x:Name="TxtCredentialUsername"/>

                <PasswordBox x:Name="TxtCredentialPassword"/>
            </StackPanel>
        </ContentDialog>
    </Grid>
</Page>