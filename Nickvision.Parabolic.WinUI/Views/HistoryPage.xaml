<Page
    x:Class="Nickvision.Parabolic.WinUI.Views.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nickvision.Parabolic.WinUI.Views"
    xmlns:nickvision="using:Nickvision.Desktop.WinUI.Controls"
    xmlns:community="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Loaded="Page_Loaded">

    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="LblHistory" Grid.Row="0" Margin="24,24,0,0" Style="{ThemeResource SubtitleTextBlockStyle}"/>

        <Grid Grid.Row="1" Margin="24,0,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <CommandBar Grid.Column="0" DefaultLabelPosition="Right">
                <CommandBar.PrimaryCommands>
                    <AppBarButton x:Name="BtnSort">
                        <AppBarButton.Icon>
                            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE8CB;"/>
                        </AppBarButton.Icon>

                        <AppBarButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <RadioMenuFlyoutItem x:Name="TglSortNewest" GroupName="Sort" Click="TglSort_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglSortOldest" GroupName="Sort" Click="TglSort_Click"/>
                            </MenuFlyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>

                    <AppBarButton x:Name="BtnLength">
                        <AppBarButton.Icon>
                            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xEC92;"/>
                        </AppBarButton.Icon>

                        <AppBarButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <RadioMenuFlyoutItem x:Name="TglLengthNever" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthOneDay" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthOneWeek" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthOneMonth" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthThreeMonths" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthSixMonths" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthOneYear" GroupName="Length" Click="TglLength_Click"/>

                                <RadioMenuFlyoutItem x:Name="TglLengthForever" GroupName="Length" Click="TglLength_Click"/>
                            </MenuFlyout>
                        </AppBarButton.Flyout>
                    </AppBarButton>
                </CommandBar.PrimaryCommands>
            </CommandBar>

            <StackPanel Grid.Column="2" HorizontalAlignment="Right" Orientation="Horizontal" Spacing="6">
                <Button x:Name="BtnClearAll" Style="{ThemeResource AccentButtonStyle}" Click="ClearAll">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontFamily="{StaticResource SymbolThemeFontFamily}" Glyph="&#xE74D;" FontSize="18"/>

                            <TextBlock x:Name="LblClearAll"/>
                        </StackPanel>
                    </Button.Content>
                </Button>
            </StackPanel>
        </Grid>

        <AutoSuggestBox x:Name="TxtSearch" Grid.Row="2" Margin="24,0,24,0" HorizontalAlignment="Stretch" QueryIcon="Find" TextChanged="TxtSearch_TextChanged"/>

        <nickvision:ViewStack x:Name="ViewStack" Grid.Row="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <nickvision:ViewStack.Pages>
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical" Spacing="24">
                    <ProgressRing IsActive="True"/>

                    <TextBlock x:Name="LblLoading" HorizontalAlignment="Center" Foreground="Gray"/>
                </StackPanel>

                <nickvision:StatusPage x:Name="StatusNone" Glyph="&#xE81C;"/>

                <nickvision:StatusPage x:Name="StatusNoneSearch" Glyph="&#xE721;"/>

                <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ListView x:Name="ListDownloads" Margin="6,0,6,0" SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <community:SettingsCard Margin="0,0,0,6" Header="{Binding Label, Mode=OneWay}" Description="{Binding Value.Url, Mode=OneWay}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <TextBlock Margin="0,0,6,0" VerticalAlignment="Center" Foreground="Gray" Text="{Binding Value.DownloadedOn, Mode=OneWay}"/>

                                        <Button Click="DownloadAgain" Tag="{Binding Value.Url, Mode=OneWay}">
                                            <Button.Content>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE896;" FontSize="18"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Click="Play" Tag="{Binding Value.Path, Mode=OneWay}" IsEnabled="{Binding Value.ExistsOnDisk, Mode=OneWay}">
                                            <Button.Content>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE768;" FontSize="18"/>
                                            </Button.Content>
                                        </Button>

                                        <Button Click="Remove" Tag="{Binding Value.Url, Mode=OneWay}">
                                            <Button.Content>
                                                <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" Glyph="&#xE894;" FontSize="18"/>
                                            </Button.Content>
                                        </Button>
                                    </StackPanel>
                                </community:SettingsCard>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </ScrollViewer>
            </nickvision:ViewStack.Pages>
        </nickvision:ViewStack>
    </Grid>
</Page>